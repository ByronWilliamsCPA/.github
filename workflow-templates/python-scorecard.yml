# Python Project - OpenSSF Scorecard
# Evaluates repository security health using OpenSSF Scorecard
#
# This workflow:
#   - Runs OpenSSF Scorecard analysis
#   - Uploads SARIF to GitHub Security tab
# Note: publish-results is always treated as false by the reusable workflow.
# See python-scorecard.yml Known Limitations for details.
#
# Triggered by:
#   - Push to main branch
#   - Weekly schedule (Sunday at 00:00 UTC)

name: OpenSSF Scorecard

on:
  push:
    branches:
      - main
  schedule:
    # Run weekly on Sunday at 00:00 UTC
    - cron: '0 0 * * 0'

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  scorecard:
    name: Scorecard Analysis
    uses: ByronWilliamsCPA/.github/.github/workflows/python-scorecard.yml@58fbe6e08975bf4b276a960cd62608a443d2f8d6 # v7.0.21
    with:
      upload-sarif: true
    permissions:
      security-events: write
      contents: read
      actions: read
